<div class="row">
  <h3>Meine Baustunden</h3>
  <div class="column">
    <table class="table">
	  <thead class="table header">
			<tr>
			  <th>Datum</th>
			  <th>Beschreibung</th>
			  <th>Dauer</th>
			  <th>Projekt</th>
			  <th>Zuständige</th>
			  <th>Bestätigt</th>									  
	    </tr>
	  </thead>
      <tbody>
        <% workunits.each do |workunit| %>
          <tr>
            <%= render workunit %>
          </tr>
        <% end %>
        <tr>
          <td colspan="6">
            <%= link_to "+ Neuer Eintrag", new_workunit_path%>
          </td>
        </tr>
      </tbody>
      <tfoot>
        
        <tr>
          <% if workunits.count > 0%>
          <td colspan="3">Bestätigte Stunden: <%= current_user.total_hours(true)%></td>
          <td colspan="3">Noch nicht Bestätigte Stunden: <%= current_user.total_hours(false)%></td>
          <%else%>
          <td colspan="6">Keine Baustunden!</td>
          <%end%>
        </tr>
        
      </tfoot>
    </table>
  </div>
</div>